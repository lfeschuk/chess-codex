import { SampleBookPage } from './types';
import { UNIFIED_PLAYBOOK_PAGES } from './unifiedPlaybookData';

// Expose the complete, unified, chronologically-sorted playbook containing 110 games, theory sections, and exercises
export const SAMPLE_PAGES: SampleBookPage[] = UNIFIED_PLAYBOOK_PAGES;
